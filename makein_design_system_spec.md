# マケイン風 デザインシステム仕様書

マケイン風デザインを生成する際に参照する仕様書。個人創作用の非公式デザインシステム。判断に迷った場合は本仕様の「組み合わせルール」を優先すること。

## 1. 基本コンセプト

- ライトノベル「マケイン」の表紙デザインをモチーフにしたポップな幾何学スタイル。
- キャラクターごとに「メインカラー」「サブカラー」の2色が固定で割り当てられている。
- 三角形・ひし形（四角形）を、枠線のみ／ドット塗り／しましま塗りの3種類の塗り方で使い分け、背景に散らして賑やかさを出す。
- 文字は太字＋黒フチ＋ドロップシャドウでポップ体風の強さを出す。

## 2. キャラクターカラーパレット

| キャラクター | 役割 | メインカラー (HEX) | サブカラー (HEX) | 配色イメージ |
|---|---|---|---|---|
| キャラA | メインキャラクター（全体のキーカラー） | `#1F4E8C`（紺碧ブルー） | `#F4B942`（黄色） | 元気で目立つブルー×イエロー |
| キャラB | サブキャラクター | `#C6B41E`（レモンイエロー） | `#2E9C93`（ターコイズ） | 活発でスポーティ、citrus×海 |
| キャラC | サブキャラクター | `#6B2333`（ワインレッド） | `#E8952C`（マスタード） | 落ち着きつつ差し色として強い |

共通色（キャラクターに紐付かない基礎色）:

| 用途 | HEX | 備考 |
|---|---|---|
| 背景紙色（ペーパーホワイト） | `#FAF5E9` | 本文・余白のベース |
| 背景紙色（濃め） | `#F2EAD8` | カードの下地などに使用 |
| 黒（インク） | `#221C16` | 縁取り・文字・境界線に使用。純黒 `#000000` は使わない |

## 3. 幾何学模様ルール

図形は以下の2種類のみを基本形とする。

1. **三角形**（正三角形寄り、頂点は上向き基準）
2. **ひし形／四角形**（正方形を45度回転させたもの）

塗り方は必ず以下の3種類のいずれかを使う。それ以外のグラデーションや複雑な塗りは使わない。

| 塗り方 | 説明 | 用途の目安 |
|---|---|---|
| 枠線のみ（outline） | `fill: none` + 太めのストローク（3〜4px） | 一番小さい装飾・背景の奥側に散らす |
| ドット塗り（dot-fill） | 同色の小さい円を等間隔に敷き詰めたパターン塗り | バッジ背景・中サイズの装飾 |
| しましま塗り（stripe-fill） | 45度斜めの縞模様パターン塗り | 見出しバー・区切り線・目立たせたい装飾 |

### SVGパターン実装例（コピー用）

```html
<defs>
  <!-- ドット塗り: colorは対象キャラのメイン or サブカラーに置き換える -->
  <pattern id="dots-{color}" width="9" height="9" patternUnits="userSpaceOnUse">
    <circle cx="2.2" cy="2.2" r="1.8" fill="{color}"/>
  </pattern>

  <!-- しましま塗り -->
  <pattern id="stripes-{color}" width="10" height="10" patternUnits="userSpaceOnUse" patternTransform="rotate(45)">
    <rect width="5" height="10" fill="{color}"/>
  </pattern>
</defs>

<!-- 三角形（枠線のみ） -->
<polygon points="55,10 100,90 10,90" fill="none" stroke="#221C16" stroke-width="4"/>

<!-- 三角形（ドット塗り） -->
<polygon points="55,10 100,90 10,90" fill="url(#dots-{color})" stroke="#221C16" stroke-width="4"/>

<!-- ひし形（しましま塗り） -->
<rect x="20" y="20" width="60" height="60" fill="url(#stripes-{color})" stroke="#221C16" stroke-width="4" transform="rotate(45 50 50)"/>
```

## 4. コンポーネントパターン

### 4-1. カバー／バナー構成

- 背景色 = 登場させるキャラクターのメインカラーを全面塗り。
- 図形装飾（枠線・ドット・しましま）をサブカラー＋紙白でランダムに散らす。四隅と余白に3〜6個程度が目安、詰め込みすぎない。
- タイトルテキストはサブカラー、または紙白で中央下〜下部に配置し、黒フチ＋ドロップシャドウを付与。
- キッカー（作品名・シリーズ名などの小ラベル）はサブカラー背景・黒文字の帯を、タイトル近くにわずかに斜め回転（-2°程度）させて添える。

### 4-2. 見出しバー（章とびら風）

- 背景 = インク色（黒）またはメインカラーの濃色。
- テキストは紙白、太字、字間をやや広め。
- バー右側30〜40%程度にしましま塗りパターンを重ねてアクセントにする。

### 4-3. キャラクターバッジ

- 白背景・黒フチの角丸ピル型。
- 左端にそのキャラのメインカラーの小さい四角ドット（アイコン代わり）を添える。
- 複数キャラを並べる場合は横並びでよい。

## 5. 組み合わせルール（AIが従うべき制約）

1. 1つの画面・1つの構図内で「メインで扱うキャラクター」は1人までとする。他キャラの色を使う場合はバッジなど小さい要素に限定する。
2. 塗り方（枠線／ドット／しましま）は同一画面内で最大2種類までに絞る。3種類同時使用は避ける。
3. 図形の色は必ずそのシーンの主役キャラのメインカラーかサブカラーのどちらかを使う。パレットにない色を新たに作らない。
4. 縁取り・文字色に使う黒は必ず `#221C16`（インク色）を使用し、純黒 `#000000` は使わない。
5. 背景の紙色は `#FAF5E9` を基本とし、カード等の下地には `#F2EAD8` を使う。
6. 文字はゴシック体・太字を基本とし、黒フチ＋ドロップシャドウでポップな強さを出す。

## 6. 実装用CSS変数（コピー用）

```css
:root{
  --paper:#FAF5E9;
  --paper-deep:#F2EAD8;
  --ink:#221C16;

  /* キャラA（メイン） */
  --char-a-main:#1F4E8C;
  --char-a-sub:#F4B942;

  /* キャラB */
  --char-b-main:#C6B41E;
  --char-b-sub:#2E9C93;

  /* キャラC */
  --char-c-main:#6B2333;
  --char-c-sub:#E8952C;
}
```

## 7. 関連ファイル

本仕様の視覚見本は `makein_design_system.html`（HTMLスタイルガイド）を参照。
